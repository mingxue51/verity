<android.support.v7.widget.Toolbar
    style="@style/ToolBarStyle"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#c50028"
    android:minHeight="@dimen/abc_action_bar_default_height_Yanrial">

    <TextView
        android:id="@+id/toolbar_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Toolbar"
        android:textColor="#FFF"
        android:textSize="20sp"/>

    <RelativeLayout
        android:id="@+id/btnBell"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:padding="16dp">

        <ImageView
            android:id="@+id/bell_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:background="@drawable/bell_icon"/>

        <TextView
            android:id="@+id/notificationCount"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_alignRight="@id/bell_icon"
            android:layout_marginRight="-2dp"
            android:layout_marginTop="-2dp"
            android:background="@drawable/notification_number_bg"
            android:gravity="center"
            android:text="0"
            android:textColor="@color/myPrimaryColor"
            android:textSize="8sp"/>

    </RelativeLayout>

</android.support.v7.widget.Toolbar>

